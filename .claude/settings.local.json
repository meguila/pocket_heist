{
  "permissions": {
    "allow": [
      "Bash(Git init)",
      "WebSearch",
      "Bash(Git switch:*)",
      "Bash(ls:*)",
      "Bash(npx vitest:*)",
      "mcp__firebase__firebase_get_environment",
      "mcp__firebase__firebase_login",
      "mcp__firebase__firebase_create_project",
      "mcp__firebase__firebase_list_projects",
      "mcp__firebase__firebase_update_environment",
      "mcp__firebase__firebase_create_app"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -NoProfile -Command \"Add-Content -Path tool-use.json -Value ('// ' + (Get-Date -Format 'yyyy-MM-dd HH:mm:ss')); $json = $input | ConvertFrom-Json; $json.tool_input | ConvertTo-Json | Add-Content -Path tool-use.json -Encoding UTF8; Add-Content -Path tool-use.json -Value ''\""
          }
        ]
      }
    ]
  }
}
